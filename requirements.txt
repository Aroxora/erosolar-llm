# Core ML
torch>=2.0.0
numpy>=1.24.0
pyyaml>=6.0

# Training
unsloth>=2024.1
transformers>=4.36.0
datasets>=2.14.0
accelerate>=0.25.0
bitsandbytes>=0.41.0
trl>=1.0      # finetune scripts use the 1.x API (max_length, processing_class)
peft>=0.7.0

# API & Async
aiohttp>=3.9.0
openai>=1.0.0
tiktoken>=0.5.0

# Monitoring
wandb>=0.16.0

# Utilities
huggingface_hub>=0.19.0
scikit-learn

# Agentic outreach (optional) — Firestore persistence for the outreach worker.
# The worker degrades to a local JSON store if this is absent; IMAP/SMTP use the
# stdlib. Install where the worker + Proton Bridge run.
firebase-admin>=7.4.0